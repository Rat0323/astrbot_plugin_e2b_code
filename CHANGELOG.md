# Changelog

## v1.0.1

### 新增

- **`/e2b plan` 指令** — 查看当前 E2B 账户计划类型和资源限制
- **计划类型配置** — 在插件设置中选择 `hobby`（免费）或 `pro`（付费），插件会据此显示对应的并发和时长限制
- **代码长度限制** — 新增 `max_code_length` 配置项，默认限制单次提交 10000 字符，防止滥用
- **并发会话上限** — 新增 `max_sessions` 配置项，可设置最大同时运行的沙箱数量（免费用户建议 20，付费用户建议 100）

### 变更

- **指令升级为指令组** — 所有指令统一使用 `/e2b` 前缀，避免与其他插件冲突
  - `/code` → `/e2b run`
  - `/code_reset` → `/e2b reset`
  - `/code_help` → `/e2b help`
  - 新增 `/e2b plan`
- **配置项优化** — 在 WebUI 插件设置中，配置项排列更合理，提示信息更清晰
- **元数据增强** — 添加 `short_desc` 短描述和 `astrbot_version` 版本要求

### 修复

- **会话内存泄漏** — 长时间运行后内存不再无限增长。现在闲置会话会自动清理，且总并发数有上限保护
- **移除不必要的依赖** — 不再需要 `httpx` 库，插件更轻量

## v1.0.0

### 功能

- **云端代码执行** — 在聊天中发送 `/code` 即可在 E2B 安全沙箱中运行代码
- **多语言支持** — 默认 Python，同时支持 JavaScript、TypeScript、R、Java、Bash
- **图表自动提取** — 使用 matplotlib 等库绘图时，图片会自动发送到聊天
- **会话持久化** — 同一会话内变量和导入的模块会自动保留
- **多维输出** — 标准输出、错误信息、返回结果分别格式化显示
- **WebUI 配置** — 在 AstrBot 管理后台直接配置 API Key、超时时间等参数
